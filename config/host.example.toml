[application]
log_level = "INFO"

[server]
# Direct USB gadget address; do not bind the service to every network interface.
bind_host = "192.168.7.1"
port = 8765

# Authentication is intentionally absent. A future token must come from an environment
# variable or a local ignored config file; never commit it here.
